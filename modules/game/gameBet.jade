.financing_props
    .financing_props_tit.cl.mt_2
        p.fl.mt_2
          span.red(data-module="countdown",data-seconds=countdown)
          | 后结束
        p.fr
          span.get_drill.fl
            - var imgSrc = window.resUrl + 'images/player3/blue_drill.png'
            if bidtype == '1'
              - imgSrc = window.resUrl + 'images/prop_icon/yellow_goldimg.png'
            img(src=imgSrc)
            = bid
          a.w70.btn_4y.fl.btn_raise(href='javascript:;')
            | &#x96C6;&nbsp;&nbsp;&#x8D44;
      .nostart_bet_con
        .scrollbars.game_scroll
          .scrollbar
            .scroll_up
            .track
              .thumb
                .thumb_end
            .scroll_down
          .viewport(style='height: 110px;')
            .overview
              p.cl= '押注内容：' + content
              ul.nostart_bet_list.cl
                -var i = 0;
                while i <= 20
                  - i++;
                  - var field = locals['field' + i];
                  if field && field.c
                    li(class='option' + (i == 1 ? ' on' : ''),data-optionid=i)= field.c
                      span(class='the_odds' + (i > 1 ? ' hide' : ''))!= '&#xFF08;&#x8D54;&#x7387;1:' + field.bet + '&#xFF09'