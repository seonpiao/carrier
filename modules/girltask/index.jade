if type==1 || type==2 || type==7 || type==8 || type==9
	if type==1
		if status==0
			li.task_titleline.cl(typeid= type,numid= id,status= status)
				.goddess_single.cl
					em#girlTask_beginTime= starttime
					span.girlTask_title= '上课：' + name
					if ready==1
						span.girltask_start.video_kankan.fr= "即将开始"
		else if status==1
			li.task_titleline.cl(typeid= type,numid= id,status= status)
				.goddess_single
					em#girlTask_beginTime= starttime
					span.girlTask_title= '上课：' + name
					if(havecg==1)
						a.girlTask_playbackurl.blue1.fr.back_lookbtn(href="' + url + '")= "回看"
		else if status==2
			li.tips_redimg.cl(typeid= type,numid= id,status= status)
				.goddess_single
					span.video_kankan.fl
						em#girlTask_beginTime= starttime
						span.girlTask_title= '上课：' + name
					span.blue.fr= "正在直播"
		else
	else if type==2
		if status==0
			li.task_titleline.cl(typeid= type,numid= id,status= status)
				.goddess_single.cl
					em#girlTask_beginTime= starttime
					span.girlTask_title= '小运动：' + name
					if ready==1
						span.girltask_start.video_kankan.fr= "即将开始"
		else if status==1
			li.task_titleline.cl(typeid= type,numid= id,status= status)
				.goddess_single
					em#girlTask_beginTime= starttime
					span.girlTask_title= '小运动：' + name
					if(havecg==1)
						a.girlTask_playbackurl.blue1.fr.back_lookbtn(href="' + url + '")= "回看"
		else if status==2
			li.tips_redimg.cl(typeid= type,numid= id,status= status)
				.goddess_single
					span.video_kankan.fl
						em#girlTask_beginTime= starttime
						span.girlTask_title= '小运动：' + name
					span.blue.fr= "正在直播"
		else
	else
		if status==0
			li.task_titleline.cl(typeid= type,numid= id,status= status)
				.goddess_single.cl
					em#girlTask_beginTime= starttime
					span.girlTask_title= name
					if ready==1
						span.girltask_start.video_kankan.fr= "即将开始"
		else if status==1
			li.task_titleline.cl(typeid= type,numid= id,status= status)
				.goddess_single
					em#girlTask_beginTime= starttime
					span.girlTask_title= name
					if(havecg==1)
						a.girlTask_playbackurl.blue1.fr.back_lookbtn(href="' + url + '")= "回看"
		else if status==2
			li.tips_redimg.cl(typeid= type,numid= id,status= status)
				.goddess_single
					span.video_kankan.fl
						em#girlTask_beginTime= starttime
						span.girlTask_title= name
					span.blue.fr= "正在直播"
if type==3 || type==4
	if type==3
		if status==0
			li.task_titleline.cl(typeid= type,numid= id,status= status)
				.goddess_single.cl
					em#girlTask_beginTime= starttime
					span.girlTask_title= '线索游戏：' + name
					if ready==1
						span.girltask_start.video_kankan.fr= "即将开始"
		else if status==1
			li.task_titleline.cl(typeid= type,numid= id,status= status)
				.goddess_single
					em#girlTask_beginTime= starttime
					span.girlTask_title= '线索游戏：' + name
					if(havecg==1)
						a.girlTask_playbackurl.blue1.fr.back_lookbtn(href="' + url + '")= "回看"
		else if status==2
			li.tips_redimg.cl(typeid= type,numid= id,status= status)
				.goddess_single
					span.video_kankan.fl
						em#girlTask_beginTime= starttime
						span.girlTask_title= '线索游戏：' + name
					span.blue.fr= "正在直播"
		else
	else if type==4
		if status==0
			li.task_titleline.cl(typeid= type,numid= id,status= status)
				.goddess_single.cl
					em#girlTask_beginTime= starttime
					span.girlTask_title= '游戏：' + name
					if ready==1
						span.girltask_start.video_kankan.fr= "即将开始"
		else if status==1
			li.task_titleline.cl(typeid= type,numid= id,status= status)
				.goddess_single
					em#girlTask_beginTime= starttime
					span.girlTask_title= '游戏：' + name
					if(havecg==1)
						a.girlTask_playbackurl.blue1.fr.back_lookbtn(href="' + url + '")= "回看"
		else if status==2
			li.tips_redimg.cl(typeid= type,numid= id,status= status)
				.goddess_single
					span.video_kankan.fl
						em#girlTask_beginTime= starttime
						span.girlTask_title= '游戏：' + name
					span.blue.fr= "正在直播"
		else
	else
	
	if status==1
		li.cl(typeid= type,numid= id,status= status)
			if type==4
				if(girlrank != null && girlrank > 0)
					.task_titleline
						span#girlTask_girlname.girlName.name_color= girlname
						span.ml_5#girlTask_rank= '获得排名：' + girlrank
			.girlrak_Lm.ml_10
				if type==4
					p   
						if (popular>0 && popular != null)
							span#girlTask_gameResult= '人气值：' + popular 
						if (Happiness>0 && Happiness != null)
							span#girlTask_gameResult= '幸福度：' + Happiness
						if (dreamfunds>0 && dreamfunds != null)
							span#girlTask_gameResult.ml_5= '梦想基金：' + dreamfunds
				if(rank && rank != null)
					for value,key in rank
						if key=="rank1"
							p#girlTask_userRank1.crown_yellow= "1st："
								span#girlTask_userRank1name= value.username
						if key=="rank2"
							p#girlTask_userRank2.crown_gray= "2nd："
								span#girlTask_userRank2name= value.username
						if key=="rank3"
							p#girlTask_userRank3.crown_orange= "3rd："
								span#girlTask_userRank3name.yellow= value.username
				if myrank
					if myrank=0 || myrank<1
						p#girlTask_myRank.crown_no.red= "您未参与此次活动"
					else if myrank>3
						p#girlTask_myRank.crown_no.yellow= myrank
							span#girlTask_myRankName= username
				
if type==5
	if status==0
		li.task_titleline.cl(typeid= type,numid= id,status= status)
			.goddess_single
				if subtype==1
					em.girlTask_beginTime= starttime
					span.girlTask_title= name
				else if subtype==2
					em.girlTask_beginTime= starttime
					span.girlTask_title= name
				else if subtype==3
					em.girlTask_beginTime= starttime
					span.girlTask_title= name
				else
				if ready==1
					span.girltask_start.video_kankan.fr= "即将开始"
	else if status==1
		li.task_titleline.cl(typeid= type,numid= id,status= status)
			.goddess_single
				if subtype==1
					em.girlTask_beginTime= starttime
					span.girlTask_title= name
				else if subtype==2
					em.girlTask_beginTime= starttime
					span.girlTask_title= name
				else if subtype==3
					em.girlTask_beginTime= starttime
					span.girlTask_title= name
				else
				if(havecg==1)
					a.girlTask_playbackurl.blue1.fr.back_lookbtn(href="' + url + '")= "回看"
	else if status==2
		li.tips_redimg.cl(typeid= type,numid= id,status= status)
			.goddess_single
				span.video_kankan.fl
					if subtype==1
						em.girlTask_beginTime= starttime
						span.girlTask_title= name
					else if subtype==2
						em.girlTask_beginTime= starttime
						span.girlTask_title= name
					else if subtype==3
						em.girlTask_beginTime= starttime
						span.girlTask_title= name
					else
				span.blue.fr= "正在直播"
	if(food && food != null)
		li.cl(typeid= type,numid= id,status= status)
			.task_titleline
				span#girlTask_girlname.girlName.name_color= girlname
				span.ml_5= "吃到"
				span(class="quality" + quality).ml_5= food
			if(rank)
				.girlrak_Lm.ml_10
					for value,key in rank
						if key=="rank1"
							p#girlTask_userRank1.crown_yellow= "1st："
								span#girlTask_userRank1name= value.username
						if key=="rank2"
							p#girlTask_userRank2.crown_gray= "2nd："
								span#girlTask_userRank2name= value.username
						if key=="rank3"
							p#girlTask_userRank3.crown_orange= "3rd："
								span#girlTask_userRank3name.yellow= value.username
					if myrank
						if myrank=0 || myrank<1
							p#girlTask_myRank.crown_no.red= "您未参与此次活动"
						else if myrank>3
							p#girlTask_myRank.crown_no.yellow= myrank
								span#girlTask_myRankName= username
if type==6
	if status==0
		li.task_titleline(typeid= type,numid= id,status= status)
			.goddess_single
				em.girlTask_beginTime= starttime
				span.girlTask_title= name
				if ready==1
					span.girltask_start.video_kankan.fr= "即将开始"
				else
					span#go_wish.girltask_start.green.fr= "去许愿"
	else if status==1
		li.task_titleline(typeid= type,numid= id,status= status)
			.goddess_single
				em.girlTask_beginTime= starttime
				span.girlTask_title= name
				if havecg==1
					a.girlTask_playbackurl.blue1.fr.back_lookbtn(href="' + url + '")= "回看"
		if defaultstr && defaultstr != null
			li#wishCon.task_titleline(typeid= type,numid= id,status= status)
				span.girlName.name_color= girlName
				span.ml_5= defaultstr
	else if status==2
		li.tips_redimg.cl(typeid= type,numid= id,status= status)
			.goddess_single.cl
				span.video_kankan.fl
					em.girlTask_beginTime= starttime
					span.girlTask_title= name
				span.blue.fr= "正在直播"
if type==10
	if status==0
		li.task_titleline(typeid= type,numid= id,status= status)
			.goddess_single
				em.girlTask_beginTime= starttime
				span.girlTask_title= name
				if ready==1
					span.girltask_start.video_kankan.fr= "即将开始"
	else if status==1
		li.task_titleline(typeid= type,numid= id,status= status)
			.goddess_single
				span.video_kankan
					em.girlTask_beginTime= starttime
					span.girlTask_title= name
				if havecg==1
					a.girlTask_playbackurl.blue1.fr.back_lookbtn(href="' + url + '")= "回看"
		if dreamfunds && dreamfunds != null
			li#wishCon.task_titleline(typeid= type,numid= id,status= status)
				span.girlName.name_color= girlName
				span.ml_5= "被投杀"
				span.ml_5= "梦想基金：" + dreamfunds
	else if status==2
		li.tips_redimg.cl(typeid= type,numid= id,status= status)
			.goddess_single.cl
				span.video_kankan.fl
					em.girlTask_beginTime= starttime
					span.girlTask_title= name
				span.blue.fr= "正在直播"